
class postDataStructure {
    constructor(title, content, userName, userEmail, website, file) {
        this.title = title,
        this.content = content,
        this.userName = userName,
        this.userEmail = userEmail,
        this.website = website,
        this.file = file
    }
}

export default postDataStructure